const INITIAL_STATE = {
  name: '',
  assertions: '',
  score: '',
  gravatarEmail: '',
};

const player = (state = INITIAL_STATE, { type }) => {
  switch (type) {
  default:
    return state;
  }
};

export default player;
